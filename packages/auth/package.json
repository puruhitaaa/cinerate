{
  "name": "@cinerate/auth",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "auth:generate": "SKIP_ENV_VALIDATION=true npx @better-auth/cli generate --config ./src/index.ts"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@cinerate/config": "workspace:*"
  },
  "dependencies": {
    "better-auth": "catalog:",
    "@better-auth/expo": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@cinerate/env": "workspace:*",
    "@cinerate/db": "workspace:*"
  }
}
